/**
 * © Panov Vitaly 2023 - All Rights Reserved
 *
 * Unauthorized copying of this file, via any medium is strictly prohibited
 * Proprietary and confidential
 * Written by Panov Vitaly <vetalpanov@gmail.com>, July 2017
 */

#include "Gun.h"
#include "GameWorld.h"
#include <vector>

using namespace SpaceBattle;

/*Gun::Gun(stringc dataDir, irrBulletWorld* world, ISceneManager* smgr, IVideoDriver* driver, IAnimatedMeshSceneNode* parent){
	this->dataDir = dataDir;
	this->sceneManager = smgr;
	this->world = world;
	this->driver = driver;
	this->parent = parent;

	this->node = this->sceneManager->addEmptySceneNode(this->parent);
	this->node->setPosition(vector3df(0, 0, this->parent->getTransformedBoundingBox().getExtent().Z + 100));
}

ISceneNode* Gun::getNode(){
	return this->node;
}*/
